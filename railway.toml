[project]
name = "holo-next-app"

[build]
# Run in monorepo root (.main), install deps, then build holo app
command = "npx -y @redplanethq/holo build"

[deploy]
# Start the Next app from its folder and bind to Railway's PORT
command = "cd .main/apps/holo && pnpm start"
