{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [ "npx -y @redplanethq/holo build" ]
    }
  },
  "deploy": {
    "inputs": [ { "step": "build" } ],
    "startCommand": "cd .main/apps/holo && node --version &&  npm run start"
  }
}