{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [ "...", {
          "src": "*",
          "dest": "/app/*"
        } ]
    }
  },
  "deploy": {
    "base": {
      "image": "docker.io/redplanethq/holo:latest"
    },
    "inputs": [
      {
        "step": "build"
      }
    ],
    "startCommand": "ls && node --version && cd /app/apps/holo && node server.js"
  }
}